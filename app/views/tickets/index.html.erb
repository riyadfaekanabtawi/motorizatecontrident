  <% if current_user %> 
<div class="row">
        <div class="col-xs-12">
          <div class="box">
            <div class="box-header">
              <h3 class="box-title">Listado de Tickets en TANG</h3>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
              <table id="example2" class="table table-bordered table-hover">
                <thead>
                <tr>
<th>Codigo de Participacion</th>
                  <th>Numero de Ticket</th>
                  <th>Sucursal de Ticket</th>
                  <th>Fecha de Ticket</th>
<th>Monto Total</th>
                  <th>Numero de Sobres en Urna</th>
          <th>Imagen de Ticket</th>
<th>Usuario que cargo Ticket</th>
<th>Email de usuario que cargo Ticket</th>
<th></th>

                </tr>
                </thead>
                <tbody>

<% @tickets.each do |ticket| %>
                <tr>
<td><%= ticket.id %></td>
                  <td><%= ticket.ticket_number %></td>

<td><%= ticket.ticket_sucursal %></td>
    <td><%= ticket.ticket_date %></td>
<td><%= ticket.monto_total %></td>
    <td><%= ticket.ticket_calculo %></td>
            
                  <td><img class="profile-user-img img-responsive img-circle" src="<%= ticket.ticket_image.url(:small) %>" /> </td>
          <td><%= Participant.find(ticket.participant_id).nombre %></td>
<td><%= Participant.find(ticket.participant_id).email %></td>
 <td> <a href="/delete_ticket?id=<%= ticket.id %>"> Eliminar</a></td>

 
                               




                </tr>
              
<% end %>
              
                </tfoot>
              </table>
            </div>
            <!-- /.box-body -->
          </div>
          <!-- /.box -->

         
          <!-- /.box -->
        </div>
        <!-- /.col -->
      </div>



 <% else %> 

<div class="modal-danger">
      
         
              <div class="modal-header">
             
            
                <h4 class="modal-title">Oops!</h4>
              </div>
              <div class="modal-body">
                <p>No tienes acceso a esta sección</p>
              </div>
              
      
    
          <!-- /.modal-dialog -->
        </div>


 <% end %> 