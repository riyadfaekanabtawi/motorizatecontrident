	

<form style="" accept-charset="UTF-8" action="/sucursales_filter" class="new_staffer" enctype="multipart/form-data" id="new_staffer3" method="get"><div style="margin:0;padding:0;display:inline"><input name="utf8" type="hidden" value="✓"><input name="authenticity_token" type="hidden" value="P51wG/otm3BiQ7h7aZbpL5Y0MuYbYCGgtZ7C+W6l0eI=" /></div>   
		
<% @current_user ||= User.find(session[:user_id]) if session[:user_id] %>
<input type="hidden" name="user_id" value="<%= @current_user.id %>" />
								<div class="form-body">
									


                                               <div class="form-group">
						<label>Selecciona una sucursal para filtrar</label>

										<div class="input-group">
<span class="input-group-addon">
											<i class="fa fa-cubes"></i>
											</span>

     <select name="sucursal" value=params[:sucursal] class="form-control">
										

        <% @sucursales.each do |subcats| %>

      <% if subcats.nombre == params[:sucursal].to_s %>	
            <option selected value="<%= subcats.nombre %>"><%= subcats.nombre %> - <%= subcats.cliente %> - <%= subcats.direccion %></option>   

      <% else %>

<% if params[:sucursal].to_s == subcats.nombre %>
<option selected value="<%= subcats.nombre %>"><%= subcats.nombre %> - <%= subcats.cliente %> - <%= subcats.direccion %></option>

<% else  %>

<option  value="<%= subcats.nombre %>"><%= subcats.nombre %> - <%= subcats.cliente %> - <%= subcats.direccion %></option>
  <% end %>   
     <% end %>           
        <% end %>		
    </select>


</div>
										
</div>

						
								
								</div>
								<button type="submit" name="submit" class="btn btn-primary pull-right">Filtrar</button>
							</form><br><br>
	